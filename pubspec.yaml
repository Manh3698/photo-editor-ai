name: photo_editor_ai
description: AI-assisted photo editor MVP
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  image_picker: ^1.1.2
  image: ^4.5.4
  gallery_saver_plus: ^3.2.8
  path_provider: ^2.1.4
  http: ^1.5.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
